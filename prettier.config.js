const { ModalContextProvider } = require("@chakra-ui/modal");
const { m } = require("framer-motion");

const options = {
    singleQuote: true,
    endOfLine: 'lf',
    arrowParens: 'avoid',
    tabWidth: 4,
    semi: false
}
module.exports = options